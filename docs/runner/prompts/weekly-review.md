Run the WEEKLY REVIEW of the "ai trading" project's paper system - the Friday-after-close scorecard. NO trading decisions, no runner call, no book edit; only review docs. Analysis runs in the cloud sandbox; state is read from the box. Markets closed today: review the sessions that traded.

RULES. Paper, absolute: never call place_equity_order, review_equity_order, cancel_equity_order or any option/crypto order tool, not even to preview. Account lock: only the Robinhood account get_accounts marks agentic_allowed=true, resolved at run time, never by nickname. Reading is not trading. Never write code to the box; the only path you write is C:\ai-trading-inputs\<run_id>\. Never edit the scrubber, mirror.py, the runner, the engine clone or C:\ai-trading-state. Do not work around a refusal: exit 1 means no - do not edit inputs to pass, do not run the engine in the sandbox, do not write a book by hand.

STAGE: export SCAN_DIR=/home/claude/review; clone https://github.com/Ayush-Patel03/AI-trading at branch production (raw-HTTPS manifest fallback if refused); read docs/PM.md sections 3, 13, 15 and docs/BACKTEST.md section 6a from it, never claude/engine/* (stale).

INPUTS from C:\ai-trading-state via Desktop Commander read_file (never device_* tools): journals\swing|pullback|momentum.json -> pm_journal_current[_pullback|_momentum].json; books\swing|pullback|momentum.json -> paper_book[_pullback|_momentum].json; coverage\pm-coverage.json; this week's health\<date>.md; experiments\ledger.jsonl if present; and start_process powershell.exe: git -C C:\ai-trading-state log --since=<Monday> --format='%h %s' (the commit list IS the run history). From the project: the latest claude/reviews/validation-*.md, claude/reviews/backtest-2026-09-10.md; list_triggers for last_run per task. Robinhood read-only: get_equity_quotes to mark opens to Friday's close; get_equity_historicals (day, from the prior Friday) for SPY, QQQ, SMH saved raw to $SCAN_DIR/bars.json, never read into context.

RUN: cd $SCAN_DIR && python3 report.py --journals pm_journal_current.json pm_journal_current_pullback.json pm_journal_current_momentum.json --books paper_book.json paper_book_pullback.json paper_book_momentum.json --from <Monday> --to <Friday> --bars bars.json --md report.md --json report.json. Quote it (refusals histogram, house return vs SPY/QQQ/SMH at average gross, attribution by exit reason / desk / score bucket, shadow gap); never recompute by hand unless report.py is absent - then say so in section 5.

PRODUCE claude/reviews/<ISO week>.md: 1 THE WEEK IN DECISIONS per desk (every entry, fill, exit, trim, rebalance, expiry, ratchet, time stop, gate refusal, halt, UNJUDGED, sentinel included; what the manager wanted and could not do, with the gate); 2 OUTCOMES per desk (realised P&L per closed trade, opens marked to Friday, equity curve; win rate / average R only on complete round trips once n >= 30; every number PAPER); 2b THE TAPE IT WAS EXPOSED TO (report.py's tables; one sentence on whether the return is distinguishable from the exposure-scaled benchmark); 3 DESK COMPARISON with n, against the backtest's Pullback-weakest / Early-Recovery-strongest finding; 4 MODEL vs FILL ARTEFACT, keeping "the decision was right" separate from "the fill was kind"; 5 SYSTEM HEALTH (expected vs actual engine runs from coverage and manifests, aborted rows and refusal reasons, engine_sha trail, blocked docs, dead-man trips, UNPROTECTED, abandoned desks, holiday skips); 6 VALIDATION (Saturday result vs the backtest's sign); 7 VERDICT, three to five sentences: evidence FOR or AGAINST the model, which desk deserves capital, the one highest-leverage fix. Never recommend going live (PM.md section 1; live mode is not implemented). Append one line to claude/reviews/index.md (week, round trips and realised P&L per desk, house vs exposure-scaled SPY, five-word verdict); project_write report.md -> claude/reviews/report-<ISO week>.md. No activity: write it anyway, short.

BOX UNREACHABLE: use the project mirror copies (claude/pm-journal*.json, claude/paper-book*.json, claude/pm-coverage.json, written daily by the health check), say so in section 5 with their date, and project_write claude/health/<date>-weekly-review-blocked.md naming the error.

REPORT: the verdict paragraph, headline numbers per desk, the benchmark line - labelled paper/simulated throughout. Not investment advice.
