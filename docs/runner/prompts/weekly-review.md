Run the WEEKLY REVIEW of the "ai trading" project's paper system - the Friday-after-close scorecard. NO trading decisions, no runner call, no book edit; only review docs. Analysis runs in the cloud sandbox; state is read from the box. Markets closed today: review the sessions that traded.

RULES. Paper, absolute: never call place_equity_order, review_equity_order, cancel_equity_order or any option/crypto order tool, not even to preview. Account lock: only the Robinhood account get_accounts marks agentic_allowed=true, resolved at run time, never by nickname. Reading is not trading. Never write code to the box; the only path you write is C:\ai-trading-inputs\<run_id>\. Never edit the scrubber, mirror.py, the runner, the engine clone or C:\ai-trading-state. Do not work around a refusal: exit 1 or 2 means no - do not edit inputs to pass, do not write a book by hand. The review REFUSES win rate and Sharpe: report.py prints neither, render_review.py exits 2 if either word would reach the page, and you do not compute them by hand either - every number carries its n, under 30 is "not a sample".

STAGE: export SCAN_DIR=/home/claude/review; clone https://github.com/Ayush-Patel03/AI-trading at branch production into $SCAN_DIR (raw-HTTPS manifest fallback if refused); record engine_sha; read docs/PM.md sections 3, 13, 15 and docs/BACKTEST.md section 6a from it, never claude/engine/* (stale). engine/report.py, engine/ic.py, engine/render_review.py, experiments/ledger.jsonl and docs/model-card.md are the clone's own.

INPUTS from C:\ai-trading-state via Desktop Commander read_file (never device_* tools): journals\swing|pullback|momentum.json -> journals/pm-journal[-pullback|-momentum].json; books\swing|pullback|momentum.json -> books/paper-book[-pullback|-momentum].json; coverage\pm-coverage.json; this week's health\<date>.md and health\<date>.json if present -> health.json; experiments\ledger.jsonl on the box only if it is newer than the clone's; and start_process powershell.exe: git -C C:\ai-trading-state log --since=<Monday> --format='%h %s' (the commit list IS the run history). From the project: the latest claude/reviews/validation-*.md, claude/reviews/backtest-2026-09-10.md; list_triggers for last_run per task. Robinhood read-only: get_equity_quotes to mark opens to Friday's close; get_equity_historicals (day, from four weeks before the prior Friday) for SPY, QQQ, SMH plus every symbol the week's journals name, saved raw to $SCAN_DIR/bars.json, never read into context. If the week's scan archive is staged (scans\<run_id>.json for the week -> $SCAN_DIR/records/), ic.py runs too; if not, say so and skip it.

RUN, in this order, from $SCAN_DIR:
1. python3 engine/report.py --journals journals/ --books books/ --bars bars.json --coverage coverage/pm-coverage.json --since <Monday> --counterfactual --md report.md --json review.json
2. (only with records/ staged) python3 engine/ic.py --records records/ --bars bars.json --horizons 5,10,20 --json ic.json
3. python3 engine/render_review.py --review review.json --ledger experiments/ledger.jsonl --model-card docs/model-card.md [--ic ic.json] [--health health.json] --out weekly-review.html
Quote report.md (refusals by rule/week/desk, E5 per gate with CI and n, attribution by exit reason / desk / score bucket / setup - the n is exit SLICES, the sample gate is round trips, both are printed - desk return vs exposure-adjusted SPY, shadow gap, house exposure, and the COVER-01 expected-vs-actual run table for section 5); never recompute by hand unless report.py is absent - then say so in section 5. A REFUSED from any step is reported as such in section 5, not worked around.

PRODUCE claude/reviews/<ISO week>.md: 1 THE WEEK IN DECISIONS per desk (every entry, fill, exit, trim, rebalance, expiry, ratchet, time stop, gate refusal, halt, UNJUDGED, sentinel included; what the manager wanted and could not do, with the gate); 2 OUTCOMES per desk (realised P&L per closed trade, opens marked to Friday, equity curve; every number PAPER with its n; no win rate, no Sharpe, no average R until n >= 30 on complete round trips - and then still no win rate); 2b THE TAPE IT WAS EXPOSED TO (report.py's tables; one sentence on whether the return is distinguishable from the exposure-scaled benchmark); 3 DESK COMPARISON with n, against the backtest's Pullback-weakest / Early-Recovery-strongest finding; 4 MODEL vs FILL ARTEFACT, keeping "the decision was right" separate from "the fill was kind" (the shadow ledger on the page is the fill side); 5 SYSTEM HEALTH (expected vs actual engine runs - quote report.py's coverage table, do not retype it from the coverage notes, aborted rows and refusal reasons, engine_sha trail, blocked docs, dead-man trips, UNPROTECTED, abandoned desks, holiday skips, any REFUSED above); 6 VALIDATION (Saturday result vs the backtest's sign; ic.json if it ran; the model card's live-validation n); 7 VERDICT, three to five sentences: evidence FOR or AGAINST the model, which desk deserves capital, the one highest-leverage fix. Never recommend going live (PM.md section 1; live mode is not implemented). Append one line to claude/reviews/index.md (week, round trips and realised P&L per desk, house vs exposure-scaled SPY, five-word verdict).

PUBLISH: project_write weekly-review.html -> claude/reviews/weekly-<Friday ISO date>.html (the rendered page: masthead, benchmark, attribution, refusals histogram, E5, shadow ledger, house exposure, experiment ledger, model card); project_write report.md -> claude/reviews/report-<ISO week>.md; project_write review.json -> claude/reviews/report-<ISO week>.json. No activity: write all of them anyway, short - the page renders empty states honestly.

BOX UNREACHABLE: use the project mirror copies (claude/pm-journal*.json, claude/paper-book*.json, claude/pm-coverage.json, written daily by the health check), say so in section 5 with their date, and project_write claude/health/<date>-weekly-review-blocked.md naming the error.

REPORT: the verdict paragraph, headline numbers per desk with n, the benchmark line, the link to the published page - labelled paper/simulated throughout. Not investment advice.
