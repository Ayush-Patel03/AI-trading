Run the PRE-OPEN RISK WATCH for the "ai trading" project's three paper desks on the box runner: a READ-ONLY look at the book before the open (overnight and pre-market, 04:00-09:30 ET). It changes nothing; it alerts. Your ONLY jobs: fetch quotes, tradability and positions, write them to C:\ai-trading-inputs\<run_id>\, start runner/run.py, read outcome.json, report.

HOLIDAY GUARD FIRST: fires on NYSE holidays too (2026: Nov 26, Dec 25; unsure -> WebSearch). Closed today: do nothing, one line "Market holiday - pre-open watch skipped."

RULES. Paper, absolute: never call place_equity_order, review_equity_order, cancel_equity_order or any option/crypto order tool, not even to preview. Account lock: only the Robinhood account get_accounts marks agentic_allowed=true, resolved at run time, never by nickname. Reading is not trading. Never write code to the box; the only path you write is C:\ai-trading-inputs\<run_id>\. Never edit the scrubber, mirror.py, the runner, the engine clone or C:\ai-trading-state. Do not work around a refusal: exit 1 means no - do not edit inputs to pass, do not run the engine in the sandbox, do not write a book by hand.

WHY IT CANNOT TRADE: fractional orders fill in regular hours only and every position is fractional, so a simulated extended-hours exit is a fill no account could get; watch.py has NO write path to a book. Do not ask for the PM to run.

INPUTS. read_multiple_files C:\ai-trading-state\books\swing.json, pullback.json, momentum.json; every held or working symbol. Write pm_quotes.json = raw get_equity_quotes (20 per call; extended prints arrive as last_non_reg_trade_price, the parser handles it), pm_tradability.json = raw get_equity_tradability on the agentic account (10 per call - decides whether an alert is actionable now), pm_broker.json = raw get_equity_positions. All flat: quote SPY alone so the run is recorded. Quotes may be 900 s old; start within 45 min.

BOX (detail: runner/README.md). Desktop Commander only (ToolSearch mcp__remote-devices__Desktop_Commander__*; never device_* tools). run_id = <YYYY-MM-DD ET>-watch-pre-open. create_directory C:\ai-trading-inputs\<run_id>; write_file each payload as compact single-line JSON, the RAW connector response (over 60,000 chars: rewrite then append pieces, get_file_info lineCount 1); sha256 over the exact UTF-8 text you wrote. Quotes LAST; as_of = the UTC time of that call. Write manifest.json {"as_of", "files": {name: {"sha256"}}}, then within 45 min:
start_process("C:\\ai-trading-runner\\.venv\\Scripts\\python.exe C:\\ai-trading-runner\\engine\\runner\\run.py --slot watch --desk all --session pre-open --inputs C:\\ai-trading-inputs\\<run_id> --state C:\\ai-trading-state", timeout_ms 300000)
Wait for the RUNNER line, read_file <run_id>\outcome.json: exit 0 committed/already_done, 1 refused (reason inside; the aborted coverage row is already written), 2 failed. Report from outcome.json only.

DONE: outcome.json committed with all three desks at exit_code 0; journals/watch.json changes only when something fired. No project writes, no artifact: state lives in C:\ai-trading-state, the board is rendered on the box, the 16:15 health check mirrors state back to the project.

RUNNER UNREACHABLE (start_process fails, hangs past 2 min, or no RUNNER line and no outcome.json): retry once. Still nothing: docs/runbooks/runner-unreachable.md step 2 - append the aborted row {ts, slot "watch", desk "all", aborted true, reason "runner unreachable: <error>", engine_source "session"} to C:\ai-trading-state\coverage\pm-coverage.json under today, uncommitted. Box dark (Desktop Commander down): project_write claude/health/<date>-watch-pre-open-blocked.md with the error, push, stop. Never run the engine in the sandbox instead.

EARNINGS AHEAD: any held name with next_earnings today or tomorrow, or listed by get_earnings_calendar (days 2) as reporting before today's open, goes in the notification. WHAT 08:45 DOES: a broken stop becomes a stop-out at the 08:45 price; a buy limit that traded through overnight expired at the roll and is re-decided; a book down 3%+ against yesterday's marks heads into the kill-switch calculation, struck before the first run's fills.

REPORT: all quiet -> one line "pre-open watch HH:MM ET - N positions checked across M desks, nothing fired." Otherwise one PushNotification per PUSH: line in the engine output the manifest carries (plus one if the run failed), then per desk: symbol, price, stop, move against the last decision-slot mark, exitable before the 08:45 PM or not. State the book was not changed and that the 08:45 PM acts. Say "paper" where needed.
