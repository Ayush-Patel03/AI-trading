Run the weekly SCORE VALIDATION for the "ai trading" project: does the Scan Desk score rank forward returns? No book, no runner, no trade. Analysis runs in the cloud sandbox; the archive is read from the box.

RULES. Paper, absolute: never call place_equity_order, review_equity_order, cancel_equity_order or any option/crypto order tool, not even to preview. Account lock: only the Robinhood account get_accounts marks agentic_allowed=true, resolved at run time, never by nickname. Reading is not trading. Never write code to the box; the only path you write is C:\ai-trading-inputs\<run_id>\. Never edit the scrubber, mirror.py, the runner, the engine clone or C:\ai-trading-state. Do not work around a refusal: exit 1 means no - do not edit inputs to pass, do not run the engine in the sandbox, do not write a book by hand.

CONTEXT: claude/reviews/backtest-2026-09-10.md is the standing answer - a two-year replay found Spearman -0.040 (10 sessions) and -0.045 (20), quintile spreads -0.70% / -1.10%, with atr_pct, week52_change_pct and rs_20d_vs_spy the only positive-sign fields. Read the weekly live number AGAINST it: agreement, disagreement, or too early.

PROCEDURE. 1 export SCAN_DIR=/home/claude/validate && mkdir -p $SCAN_DIR/records; clone https://github.com/Ayush-Patel03/AI-trading at branch production into $SCAN_DIR (raw-HTTPS manifest fallback if refused); record engine_sha; read docs/SCAN.md section 11 and docs/BACKTEST.md section 6a; never claude/engine/validate.py (stale). 2 Desktop Commander read_file (never device_* tools): C:\ai-trading-state\scan-index.json, then every listed C:\ai-trading-state\scans\<run_id>.json verbatim into $SCAN_DIR/records/, and archive\followed.json (the survivorship-free followed set, BACKTEST.md section 2a). Fewer than 3 distinct scan days: stop, report not enough archive, never manufacture records. 3 Tickers across all records plus open names in followed.json plus SPY: get_equity_historicals day, span from the earliest record minus 5 sessions to today, TEN per call, each raw response to a file, concatenated into one JSON list at $SCAN_DIR/bars.json - never read bars into context. 4 cd $SCAN_DIR && python3 validate.py --records records --bars bars.json --horizons 5,10,20 --out validation.json --md validation.md; then python3 ic.py --records records --bars bars.json --horizons 5,10,20 --by-feature --md ic.md --json ic.json (per-date rank IC, Newey-West t; one line if ic.py is absent). 5 project_write validation.md -> claude/reviews/validation-<ISO week>.md, validation.json -> claude/reviews/validation-<ISO week>.json, ic.md -> claude/reviews/ic-<ISO week>.md; append to claude/reviews/validation-index.md: week, n per horizon, Spearman per horizon, IC t at 20, strongest unscored field, one clause on agreement with the backtest's sign.

BOX UNREACHABLE: use the project copies claude/scan-index.json and claude/scans/<run_id>.json (written daily by the health check), say so with their date, and project_write claude/health/<date>-score-validation-blocked.md naming the error.

REPORT: per-horizon Spearman and p, the top-minus-bottom quintile spread, by-verdict hit rates, the IC 20-session row, and which unscored fields beat the score - the candidates to score next; NOTHING enters the model without appearing here AND beating the incumbent in a backtest with a ledger row. n under 30 everywhere: "not enough data yet" is correct. Paper record; forecasts nothing; not investment advice.
