Run the OPENING RANGE Scan Desk slot for the "ai trading" project on the box runner. Your ONLY jobs: collect the live inputs the box cannot, write them to C:\ai-trading-inputs\<run_id>\, start runner/run.py, read outcome.json, report.

HOLIDAY GUARD FIRST: fires on NYSE holidays too (2026: Nov 26, Dec 25; unsure -> WebSearch). Closed today: do nothing, one line "Market holiday - opening range scan skipped."

RULES. Paper, absolute: never call place_equity_order, review_equity_order, cancel_equity_order or any option/crypto order tool, not even to preview. Account lock: only the Robinhood account get_accounts marks agentic_allowed=true, resolved at run time, never by nickname. Reading is not trading. Never write code to the box; the only path you write is C:\ai-trading-inputs\<run_id>\. Never edit the scrubber, mirror.py, the runner, the engine clone or C:\ai-trading-state. Do not work around a refusal: exit 1 means no - do not edit inputs to pass, do not run the engine in the sandbox, do not write a book by hand.

SLOT FOCUS 10:00 ET (meta.slot "Opening range", meta.time "10:00"). Volume is real now, so relative volume means something. Which pre-market names CONFIRMED and which FADED, plus what only became visible after the open. Carry forward the pre-market top 8 (tickers from C:\ai-trading-state\scans\latest.json) even if faded: refresh price and volume, reuse fundamentals. A mention spike a SECOND feed corroborates on real volume is the strongest combination; single-source attention is labelled. Carry meta.macro_events forward, or one WebSearch if 08:00 did not run. No transcripts, IPO, insider or implied-move work.

INPUTS (collection only - the engine runs on the box; use the market-scan skill and docs/COLLECTION.md for how to collect, and STOP before their first python3 command). Write to C:\ai-trading-inputs\<run_id>\: scan_data.json {meta{scan_date,time,slot,data_warnings}, regime, candidates{TICKER{...}}, history} as the skill builds it (RH watchlists + saved scan 55138bd9 universe, 8-name deep dives via WebSearch then WebFetch for GICS sector / analyst / short float / catalysts / next_earnings, regime from get_index_quotes, meta.macro_events); bars.json = list of raw get_equity_historicals responses (day, year, TEN per call, SPY included); fundamentals.json = raw get_equity_fundamentals; quotes.json = raw get_equity_quotes for every candidate, fetched last; the sentiment payloads raw: apewisdom.json, st_trending.json, st_gauges.json, rh_watchlists.json, quotes_min.json, options_scan.json (saved scan cc3b6743), earnings_days.json (no reddit_posts.json - Reddit is gone); optional, when budget allows: st_symbol_<SYM>.json = the RAW api.stocktwits.com/api/2/streams/symbol/<SYM>.json response, one file per finalist, upper-case symbol in the file name (read for the Bullish/Bearish split only, never as attention), and trends.json = {"SYM": [{"date": "YYYY-MM-DD", "value": N}, ...]} from Google Trends (skip when no route works; a missing file is a null, never a zero). The rolling archive/attention_history.json is the runner's state, not an input - never write it; optional option_chains.json for the finalists (skip on error). Unlisted files are not staged.

BOX (detail: runner/README.md). Desktop Commander only (ToolSearch mcp__remote-devices__Desktop_Commander__*; never device_* tools). run_id = <YYYY-MM-DD ET>-opening-range-scan. create_directory C:\ai-trading-inputs\<run_id>; write_file each payload as compact single-line JSON, the RAW connector response (over 60,000 chars: rewrite then append pieces, get_file_info lineCount 1); sha256 over the exact UTF-8 text you wrote. Quotes LAST; as_of = the UTC time of that call. Write manifest.json {"as_of", "files": {name: {"sha256"}}}, then within 25 min:
start_process("C:\\ai-trading-runner\\.venv\\Scripts\\python.exe C:\\ai-trading-runner\\engine\\runner\\run.py --slot opening-range --step scan --desk all --inputs C:\\ai-trading-inputs\\<run_id> --state C:\\ai-trading-state", timeout_ms 900000)
Wait for the RUNNER line, read_file <run_id>\outcome.json: exit 0 committed/already_done, 1 refused (reason inside; the aborted coverage row is already written), 2 failed. Report from outcome.json only.

DONE: outcome.json committed with scan.run_id - the runner ran technicals, sentiment, scanner, mirror, render, archive, index, history and committed scans/latest.json (the PM hand-off) and the records; history_refused true is worth one line. No project writes, no artifact: state lives in C:\ai-trading-state, the board is rendered on the box, the 16:15 health check mirrors state back to the project.

RUNNER UNREACHABLE (start_process fails, hangs past 2 min, or no RUNNER line and no outcome.json): retry once. Box dark (Desktop Commander down): project_write claude/health/<date>-opening-range-scan-blocked.md with the error, push, stop. Never run the engine in the sandbox instead.

REPORT: run_id, engine_sha, technicals_merged, sentiment, history_refused from outcome.json; then notable-only from the scanner stdout the manifest carries: top names with score, setup, one reason each. Push ONLY on a refused, failed or unreachable run.
