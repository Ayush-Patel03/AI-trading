Send the MORNING BRIEF for the "ai trading" project's three paper desks at 08:45 ET - one push, one ntfy post and one email, READ-ONLY. Your ONLY jobs: run engine/brief.py against the state repo on the box, read the two files it writes, deliver them, report. It fetches nothing: the 08:00 scan is the tape and the 07:00 pre-open watch is the overnight, and both are already on disk.

HOLIDAY GUARD FIRST: fires on NYSE holidays too (2026: Nov 26, Dec 25; unsure -> WebSearch). Closed today: do nothing, one line "Market holiday - morning brief skipped."

RULES. Paper, absolute: never call place_equity_order, review_equity_order, cancel_equity_order or any option/crypto order tool, not even to preview. Account lock: only the Robinhood account get_accounts marks agentic_allowed=true, resolved at run time, never by nickname. Reading is not trading. Never write code to the box; the only path you write is C:\ai-trading-inputs\<run_id>\. Never edit the scrubber, mirror.py, the runner, the engine clone or C:\ai-trading-state. Do not work around a refusal: exit 2 means no - do not edit state to pass, do not run the engine in the sandbox, do not write a brief by hand from memory.

WHY IT IS NOT A SLOT: brief.py takes no lock, stages no inputs, runs no engine step and writes nothing into C:\ai-trading-state, so it is not a `run.py --slot`. Like runner/deadman.py it is its own program on its own schedule. A brief that wrote state would be a second writer racing the 08:45 PM slot.

BOX (detail: runner/README.md). Desktop Commander only (ToolSearch mcp__remote-devices__Desktop_Commander__*; never device_* tools). run_id = <YYYY-MM-DD ET>-morning-brief. create_directory C:\ai-trading-inputs\<run_id>. Read alerts.ntfy_topic and alerts.email from C:\ai-trading-runner\engine-config.json - the topic name is the PASSWORD for a public ntfy topic, so it is never written into this file, into the engine clone, into the state repo or into a project doc; read it at run time and use it once. Then:
start_process("C:\\ai-trading-runner\\.venv\\Scripts\\python.exe C:\\ai-trading-runner\\engine\\engine\\brief.py --state C:\\ai-trading-state --text C:\\ai-trading-inputs\\<run_id>\\brief.txt --html C:\\ai-trading-inputs\\<run_id>\\morning-brief.html --json C:\\ai-trading-inputs\\<run_id>\\brief.json --ntfy <topic url from engine-config>", timeout_ms 120000)
Exit 0 the brief was built (with or without alerts); exit 2 nothing under --state could be read at all. The ntfy result prints on stderr as "ntfy: sent" or "ntfy: NOT sent - <reason>"; a POST failure never fails the brief. EGRESS: the ntfy host needs an allowlist entry before a scheduled run can reach it (P-08, claude/plans/2026-09-11-p08-connectors.md). Until it has one, expect "NOT sent - URLError" and deliver by push and email alone - say so rather than dropping the line.

READ BACK: read_file C:\ai-trading-inputs\<run_id>\brief.txt - that is the body, already under 1,200 characters and already ordered with anything that needs a human first. Do not rewrite it, do not summarise it, do not add a number to it: every figure in it is the engine's, and a figure you add by hand is one no test covers.

DELIVER: PushNotification with the first 200 characters of brief.txt. Email the full brief.txt to alerts.email, subject "AI-trading paper brief <date>", with morning-brief.html attached or inlined. ntfy was already posted by the program. Say which channels succeeded. PRIORITY: the body's first line ends "N need you" or "nothing needs you" - push high only when N > 0, with the one exception below.

HOUSE EXPOSURE IS NOT AN EMERGENCY: a line starting "HOUSE EXPOSURE:" says the combined book carries an exposure flag - almost always N_eff under the 3.0 floor - and that enforce is OFF, so nothing was refused, no entry was skipped and no size was cut. It is an awareness class and it is ranked below every alert that can lose money today. Expect it most mornings: on the proxy basis (sector rho 1.0 inside a GICS sector, 0.3 across) N_eff cannot reach 3.0 at all - eleven equally weighted names, one per sector, measure 2.75 - so the flag is a standing reading of a small, crowded book, not an event. If HOUSE EXPOSURE is the ONLY alert, push at NORMAL priority, not high, and say in the push that nothing was refused. Do not soften it, do not sharpen it and do not drop it: the line already carries N_eff, its basis (proxy = correlation assumed, not measured) and the floor, and every one of those three must reach the reader together. An ENFORCING house exposure is a different thing entirely and arrives as "HOUSE CAP: house exposure is ENFORCING ... new entries are refused house-wide" - that one is high priority, because an entry was or will be turned away. ONE MORE THING WORTH SAYING ALOUD: the brief is the only job that raises this. The PM slots and the sentinel compute the same block every run and alert on none of it, so a breach that opens intraday waits until 08:45 the next morning - do not report an intraday n_eff move as caught, because nothing is watching it.

EARNINGS ARE A KNOWN GAP: brief.py reads next_earnings from the position row and then the scan row, and both are null for most held names today. "No held name reporting before the next open" therefore means "none that the state knows about". If the page says the earnings coverage line, cross-check by hand with get_earnings_calendar (days 2, read-only) for the held names and add ONE line to the notification naming what you found and that it came from the connector, not from the brief. That is P-06's gap, not a defect in this task.

BOX UNREACHABLE (start_process fails, hangs past 2 min, or no output): retry once. Still nothing: build nothing by hand - instead project_write claude/health/<date>-morning-brief-blocked.md with the error, push one line saying the box is dark and that the 08:45 PM slot has not been checked, and stop. docs/runbooks/box-dark.md. A brief that says the box is dark is the most useful brief there is; a brief invented from a stale project mirror is not.

NO WRITES: this task writes no book, no journal, no coverage row, no artifact and (box reachable) no project doc. brief.txt, morning-brief.html and brief.json live under C:\ai-trading-inputs\<run_id>\ and are swept with the rest of that directory.

REPORT: the brief text verbatim, the delivery result per channel, and the exit code. Paper books; not investment advice.
